#pure digit check
    # l1=["100", "3.0", "⅔", "42", "eight", ""]
    # r1=list(map(str.isdecimal, l1))
    # print(r1)
    
#decimal characters only
    # l2=["٢", "4.5", "5", "٩", "0", ""]
    # r2=list(map(str.isdecimal, l2))
    # print(r2)