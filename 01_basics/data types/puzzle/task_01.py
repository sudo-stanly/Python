#[NAME MIXER]: Task: Create a new string like this => "A.L."
    # Use string indexing and formatting

# first, last ="Ada", "Lovelace"
        # ff=first.find("A") #0
        # fl=last.find("L") #0
        # print(ff); print(fl)
# print(f"{first[0]}.{last[0]}.")